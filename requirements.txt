# Python dependencies for ORACLE tools/
# Install: pip install -r requirements.txt
# Requires Python >= 3.9 (zoneinfo is stdlib from 3.9+)

requests>=2.28.0
playwright>=1.40.0
defusedxml>=0.7.1
pyarrow>=14.0.0

# Pre-existing data/ML tools (install separately if using gbm_residual.py):
# numpy, pandas, xgboost
