# Archive — pre-refactor monolith

Historical reference only. Nothing in here is built, linted, or imported.

- `ORACLE_v2026_8_0.jsx` — the 7,134-line pre-Phase-0 monolith. All `ORACLE_v2026_8_0.jsx lines N–M` provenance comments across `packages/` point into this file.
- `oracle_core_v2026_8_0.py` — pre-port Python core.
- `oracle_math.ts` — pre-port math module; the live version is `packages/engine/src/math/index.ts`.
- `regression_harness.js` — pre-port regression harness; superseded by the vitest suites and golden tests in `packages/engine/test/`.
