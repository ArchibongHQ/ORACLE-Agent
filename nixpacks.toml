[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm turbo run build"]

[start]
cmd = "pnpm --filter @oracle/web start"
